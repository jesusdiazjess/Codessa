#include <stdio.h>

int main() {
  register int x = 0;  // Declare a register variable

  // Access the variable multiple times
  for (int i = 0; i < 1000000; i++) {
    x++;
  }

  // Print the final value of the variable
  printf("x = %d\n", x);

  return 0;
}
